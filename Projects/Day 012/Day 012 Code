const int POT_PIN = A0;
const int BUZZER_PIN = 9;

void loop() {
  int potValue = analogRead(POT_PIN);
  int beepDelay = map(potValue, 0, 1023, 50, 1000);
  tone(BUZZER_PIN, 1000, 100);
  delay(beepDelay);
}